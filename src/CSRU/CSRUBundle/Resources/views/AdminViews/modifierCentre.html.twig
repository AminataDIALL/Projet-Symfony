{% extends "::layout/layout.html_1.twig" %}
{% block body %}
    <head>
        <title> Modifier </title>
    </head>
    <div class="col-lg-2"></div>
    <div class="col-lg-8">
         <h2> Formulaire de modification</h2>
         <br/>
        {{ form_start(form) }}
        {{ form_label(form.nom) }}
        {{ form_widget(form.nom ,{'attr':{'class':'form-control'}}) }}
        {{ form_errors(form.nom) }}
        <br/><br/>
        <center> <button class="btn btn-primary" type="submit">Modifier</button>
            {{ form_end(form) }}
        <a href="{{ path('listeCentre') }}"><button class="btn btn-success">Annuler</button></a>
        </center>
    </div>
    <div class="col-lg-2"></div>
{% endblock %}
